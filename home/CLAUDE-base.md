# Global guidance for Claude Code

Configuration managed by [claude-code-nix](https://github.com/pawelwlazlo/claude-code-nix).
Per-machine overrides go in `~/.claude/settings.local.json` (not touched by the module).

This file is regenerated on every `home-manager switch` — edit options in your
`home.nix` instead of editing here directly.
